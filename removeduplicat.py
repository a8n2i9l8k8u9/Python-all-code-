#write a program to remove a duplicate from list.
ls=['RAM','ROM','RAM','PROM']
c=0
for i in ls:
    for j in i:
        if(ls=='RAM' or ls=='ROM' or ls=='PROM'):
            if(ls=='RAM'):
                print(ls)
            else:
                print("not duplicate")
